# Configuração para RECRIAR SCHEMA COMPLETAMENTE (usar apenas quando necessário)
# DESCOMENTE as linhas abaixo apenas quando quiser recriar todas as tabelas

# spring.sql.init.mode=always
# spring.sql.init.encoding=UTF-8
# spring.sql.init.platform=postgresql
# spring.sql.init.schema-locations=classpath:schema.sql
# spring.sql.init.data-locations=classpath:data.sql

# IMPORTANTE: Quando usar isso, TODOS OS DADOS serão perdidos!
# Use apenas para:
# - Primeira execução do projeto
# - Mudanças estruturais grandes no schema
# - Ambiente de teste limpo